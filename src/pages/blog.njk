---
layout: layout.njk
title: Blog
permalink: /blog/
---

<h1 class="fw-bold mb-4">Blog</h1>

{% set posts = (collections.post or []) | reverse %}
{% if posts | length == 0 %}
  <p>No posts yet. Check back soon.</p>
{% else %}
  <ul class="list-unstyled">
    {% for post in posts %}
      <li class="mb-3">
        <a class="fw-semibold" href="{{ post.url }}">{{ post.data.title }}</a>
        <div class="text-secondary small">{{ post.date | fmtDate }}</div>
      </li>
    {% endfor %}
  </ul>
{% endif %}
